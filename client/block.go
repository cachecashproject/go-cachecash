package client

/*

// Created in response to a ticket bundle.
type blockGroupRequest struct {
	blocks []*blockRequest
	bundle *ccmsg.TicketBundle
}

// Tracks a request for a single block from a single cache.
// - May receive cancellation instructions from parent blockGroupRequest.
// - May get connection error (e.g. disconnection) and report failure to parent.
// - May successfully get singly-encrypted data and notify parent that it is available.
// - May be given L2 data by parent (after colocation puzzle is solved).
type blockRequest struct {
}
*/
